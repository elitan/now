export default function CampaignPage(): React.ReactElement {
  return (
    <main className="panel" data-testid="campaign-page">
      <h1>Campaign</h1>
      <p>This route lives inside a route group and resolves to /campaign.</p>
    </main>
  );
}
